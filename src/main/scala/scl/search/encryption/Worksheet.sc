import scl.search.encryption.Solution

val sl = Math.sqrt(80)
val lb = Math.floor(sl)
val ub = Math.ceil(sl)
val g = Solution.grid(80)
val s = "if man was meant to stay on the ground god would have given us roots".replace(" ", "")
